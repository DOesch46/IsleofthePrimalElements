using UnityEngine;

/// <summary>
/// Compatibility wrapper so existing scenes that reference PlayerSpawnHandler
/// now use the shared SceneSpawnHandler logic.
/// </summary>
public class PlayerSpawnHandler : SceneSpawnHandler
{
}
